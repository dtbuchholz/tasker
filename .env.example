# Database connection string (Neon/Supabase/etc.)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Set to "false" to disable mutations (dry-run mode)
ALLOW_MUTATIONS=true
